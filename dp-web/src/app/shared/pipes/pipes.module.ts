import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RedirectTo } from './redirect-to';

@NgModule({
  imports: [CommonModule],
  declarations: [RedirectTo],
  exports: [RedirectTo],
})
export class PipesModule {}
